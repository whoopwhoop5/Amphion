# Extra dependencies to reproduce StyleStream-style objective metrics.
#
# Install into the same venv as Vevo (after torch/torchaudio).

pyarrow
speechbrain
modelscope
funasr
addict
# modelscope currently expects the pre-3.x HuggingFace `datasets` API.
datasets<3.0.0
simplejson
sortedcontainers
